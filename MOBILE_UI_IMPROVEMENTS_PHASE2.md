# 📱 모바일 UI 2단계 개선 - 팝업/툴팁/메뉴 최적화

**Date**: 2026-05-22
**Status**: ✅ **진행 중 - 팝업/메뉴 최적화**

---

## 개요

1단계에서 완료한 메인 UI (카드 선택, 게임오버, 시작 화면 등)에 이어, 2단계에서는 팝업, 툴팁, 대화창 등의 세부 UI 요소들을 모바일 환경에 최적화합니다.

---

## 📝 완료한 개선사항들

### 1️⃣ **카드 디테일 팝업** ✅
**파일**: survivor.html (Line 13615-13650)

#### 문제점
- 데스크톱: max-width 260px 고정
- 폰트 사이즈 11px (너무 작음)
- 기술 설명 padding 부족

#### 개선 사항
```javascript
// 개선 전
max-width: 260px
font-size: 11px
padding: 14px 16px
line-height: 1.5

// 개선 후 (모바일 감지)
const isMobile = window.innerWidth < 520;
max-width: ${isMobile ? 'min(90vw, 320px)' : '280px'}
font-size: ${isMobile ? '13px' : '12px'}
padding: 16px 18px (모바일에서도 동일)
line-height: 1.6 (더 나은 가독성)
```

#### 상세 변경 내용
- **팝업 너비**: 260px → 280px (데스크톱) / 320px (모바일, 최대 90vw)
- **기본 폰트**: 11px → 12px (데스크톱) / 13px (모바일)
- **카드명 폰트**: 13px → 14px
- **설명 line-height**: 1.5 → 1.6 (더 나은 간격)
- **내부 여백**: padding 6-10px → 8-10px (더 나은 공간)
- **기술 폰트**: 10px → 11px (더 읽기 쉬움)

**효과**: 모바일 화면에서 팝업이 더 크고 읽기 쉬워짐 ✓

---

### 2️⃣ **맵 노드 툴팁** ✅
**파일**: survivor.html (Line 5539, 5610)

#### 문제점
- 데스크톱: max-width 220px, font-size 12px
- 모바일: max-width 240px, font-size 13px
- 줄 높이 설정 없음 (텍스트가 붙어 있음)

#### 개선 사항
```javascript
// 데스크톱 (Line 5539)
font-size: 12px → 13px (통일)
max-width: 220px → 240px (약간 더 넓게)
padding: 10px 14px → 12px 16px (더 나은 여백)
line-height: 추가 (1.5)

// 모바일 (Line 5610)  
max-width: 240px → min(90vw, 280px) (반응형)
font-size: 13px → 유지
padding: 10px 14px → 12px 16px
line-height: 1.5 추가
```

**효과**: 모든 해상도에서 균일한 좋은 가독성 ✓

---

### 3️⃣ **일시정지 메뉴** ✅
**파일**: survivor.html (Line 14484+)

#### 문제점
- 모바일에서 3열 그리드 (HP, 골드, 속도)로 인한 혼잡
- 큰 폰트 크기 (title 22px, stat 15px)
- 큰 버튼들이 가로로 배치되어 공간 낭비

#### 개선 사항

**레이아웃**
```javascript
// 모바일 감지
const isMobile = window.innerWidth < 520;

// 메뉴 너비
width: ${isMobile ? 'min(360px, 96vw)' : 'min(440px, 96vw)'}

// 스탯 그리드 (3열 → 2열)
grid-template-columns: ${isMobile ? '1fr 1fr' : '1fr 1fr 1fr'}
gap: ${isMobile ? '6px' : '8px'}

// 버튼 레이아웃 (가로 → 세로)
${isMobile ? 'flex;flex-direction:column' : 'flex'}
gap: ${isMobile ? '6px' : '8px'}
```

**폰트 크기 조정**
| 요소 | 데스크톱 | 모바일 | 변경 |
|------|---------|--------|------|
| 제목 ("⏸ 일시정지") | 22px | 18px | -4px |
| 정보 (ACT/WAVE) | 11px | 10px | -1px |
| 스탯 레이블 | 9px | 8px | -1px |
| 스탯 값 | 15px | 13px | -2px |
| 섹션 제목 | 10px | 9px | -1px |

**간격 조정**
| 요소 | 데스크톱 | 모바일 |
|------|---------|--------|
| 패널 padding | 24px | 18px |
| 제목 margin-bottom | 18px | 12px |
| 스탯 gap | 8px | 6px |
| 버튼 gap | 8px | 6px |

**효과**: 모바일에서 컴팩트하지만 여전히 사용하기 쉬운 레이아웃 ✓

---

### 4️⃣ **레벨업 팝업 효과** ✅
**파일**: survivor.html (Line 13693-13696)

#### 문제점
- 이모지 64px, 카드명 20px (모바일에서 화면을 가득 채움)

#### 개선 사항
```javascript
const isMobileView = window.innerWidth < 520;

// 이모지 크기
font-size: ${isMobileView ? '48px' : '64px'}

// 카드명
font-size: ${isMobileView ? '16px' : '20px'}
margin-top: ${isMobileView ? '6px' : '8px'}

// 등급 텍스트
font-size: ${isMobileView ? '9px' : '10px'}
margin-top: ${isMobileView ? '2px' : '3px'}
```

| 요소 | 데스크톱 | 모바일 | 비율 |
|------|---------|--------|------|
| 이모지 | 64px | 48px | 75% |
| 카드명 | 20px | 16px | 80% |
| 등급 | 10px | 9px | 90% |

**효과**: 모바일에서도 깔끔한 레벨업 효과 애니메이션 ✓

---

## 🔄 개선 패턴

모든 변경사항에 공통적으로 적용된 패턴:

```javascript
// 1. 모바일 감지
const isMobile = window.innerWidth < 520;

// 2. 조건부 스타일 적용
style.cssText = `...size: ${isMobile ? 'mobile' : 'desktop'}...`;

// 3. 여백 최적화
padding: ${isMobile ? '작은값' : '큰값'}

// 4. 레이아웃 최적화
grid-columns: ${isMobile ? '1fr 1fr' : '1fr 1fr 1fr'}
flex-direction: ${isMobile ? 'column' : 'row'}
```

---

## 📊 모바일 vs 데스크톱 비교

### 카드 디테일 팝업
```
데스크톱 (1200px):              모바일 (375px):
┌─────────────────┐            ┌────────────┐
│ 🎭 카드명      │            │ 🎭 카드명 │
│ 설명 텍스트... │            │ 설명 텍스트│
│ 효과: 대미지   │            │ 효과: 대   │
│ ✨ 시너지 발동! │            │ ✨시너지!  │
└─────────────────┘            └────────────┘
  280px 너비                     320px 너비
  12px 폰트                      13px 폰트
```

### 일시정지 메뉴 스탯
```
데스크톱:                       모바일:
┌─┬─┬─┐                        ┌─┬─┐
│H│G│S│  (3열)                 │H│G│  (2열)
├─┼─┼─┤                        ├─┼─┤
│P│D│E│                        │S│  |
└─┴─┴─┘                        └─┴─┘
```

---

## ⚠️ 남은 개선 대상

### 1. 이벤트 선택지
- 긴 텍스트가 줄바꿈될 때 레이아웃 확인 필요
- 선택지 버튼 크기 최적화

### 2. 렐릭/아티팩트 설명 팝업
- hover 상태에서 표시되는 설명 폰트 크기
- 모바일에서는 long-press로 표시됨

### 3. 스킬 설명 툴팁
- 게임 중 스킬 레벨업 시 표시되는 설명
- 폰트 크기 및 너비 확인

### 4. 에러 메시지
- 오류 팝업의 텍스트 크기
- 확인 버튼 크기

### 5. 로딩/진행 표시기
- 진행 도트의 크기
- 로딩 텍스트의 가독성

---

## 📈 예상 효과

### 사용자 경험 개선
- ✅ 더 읽기 쉬운 텍스트 (13px 이상)
- ✅ 적절한 여백으로 답답함 없음
- ✅ 터치 타겟 크기 유지 (44px 이상)
- ✅ 반응형 레이아웃

### 성능 영향
- 없음 (CSS/JavaScript만 변경)
- 메모리 사용량 동일

### 접근성 개선
- 큰 텍스트로 시각 장애인 친화
- 충분한 대비로 가독성 향상

---

## 🧪 테스트 체크리스트

### 모바일 (375px)
- [ ] 카드 디테일 팝업 - 텍스트 겹치지 않음
- [ ] 맵 노드 툴팁 - 읽기 쉬운 크기
- [ ] 일시정지 메뉴 - 2열 그리드 확인
- [ ] 레벨업 팝업 - 이모지 크기 적절
- [ ] 버튼 정렬 - 세로 배치 확인

### 태블릿 (768px)
- [ ] 모든 팝업 - 너비 적절
- [ ] 레이아웃 - 혼잡하지 않음

### 데스크톱 (1200px)
- [ ] 모든 팝업 - 원래 크기 유지
- [ ] 성능 - 문제 없음

---

## 📝 코드 변경 요약

| 파일 | 위치 | 변경 | 라인수 |
|------|------|------|--------|
| survivor.html | 13615-13650 | 카드 팝업 최적화 | ~35 |
| survivor.html | 5539, 5610 | 맵 툴팁 최적화 | ~5 |
| survivor.html | 14484+ | 일시정지 메뉴 최적화 | ~50 |
| survivor.html | 13693-13697 | 레벨업 팝업 최적화 | ~10 |
| **합계** | - | - | **~100** |

---

## 🎯 다음 단계

### 즉시 (1-2시간)
1. ✅ 카드 디테일 팝업 최적화
2. ✅ 맵 노드 툴팁 최적화
3. ✅ 일시정지 메뉴 최적화
4. ✅ 레벨업 팝업 최적화
5. 이벤트 선택지 팝업 최적화
6. 렐릭/아티팩트 설명 최적화

### 단기 (1-2일)
1. 모든 팝업 모바일 테스트
2. 색상 대비 확인 (WCAG 기준)
3. 사용자 피드백 수집

### 장기
1. 더 세분화된 반응형 브레이크포인트 추가
2. 다크 모드 지원
3. 텍스트 사이즈 조정 옵션 추가

---

**Status**: 🟡 **진행 중 - 추가 팝업 최적화 예정**
**다음 업데이트**: 2026-05-22 (이벤트/렐릭 팝업)
